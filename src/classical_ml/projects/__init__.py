"""Three end-to-end machine learning projects, one per problem type.

Each module exposes ``run()``, writes its figures to ``outputs/<project>/`` and returns
the metrics it measured. They are independent: nothing here imports from another project.
"""
